= render 'shared/navbar'

%h1 Annonces récentes

%table
  %thead
    %tr
      %th Title
      %th Body
      %th Price
      %th Creator
      %th
      %th
      %th

  %tbody
    - @annonces.each do |annonce|
      %tr
        %td= annonce.title
        %td= annonce.body
        %td= (annonce.price/100.0)
        %td= annonce.creator_id
        %td= link_to 'Show', annonce
        %td= link_to 'Edit', edit_annonce_path(annonce)
        %td= link_to 'Destroy', annonce, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

- if user_signed_in?
  = link_to 'Poster une annonce', new_annonce_path
  %br
- else
  %h5 Vous devez vous connecter pour poster une annonce.